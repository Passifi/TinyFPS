BasedOnStyle: LLVM
SortIncludes: true
IncludeBlocks: Preserve
IncludeCategories:
  # 1) glad must come first
  - Regex: '^<glad/.*>'
    Priority: 1
  # 2) then GLFW
  - Regex: '^<GLFW/.*>'
    Priority: 2
  # 3) everything else
  - Regex: '^<.*>'
    Priority: 3
  - Regex: '^".*"'
    Priority: 4
